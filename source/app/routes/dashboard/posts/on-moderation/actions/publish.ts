// import { confirmPublishPost } from "~/shared/.server/utils/postUtils";

// export async function publishPostAction(postId: number, sessionUserId: number) {
//   return await confirmPublishPost(postId, sessionUserId);
// }
